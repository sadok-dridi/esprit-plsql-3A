# Flask secret key for session signing
SECRET_KEY=change-me-to-a-random-string

# Optional: restrict admin access to a specific username
# If not set, the first registered user (id=1) is the admin
ADMIN_USERNAME=
